const medcords = document.getElementById("medcords");
const records = document.getElementById("records-list");
const recordTitle = document.getElementById("record-title").value;
const recordFile = document.getElementById("record-file");
const uploadBtn = document.getElementById("uploadbtn");
const forms = document.getElementById("formsBox");

function upload(){
    console.log(recordTitle);
}